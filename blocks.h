struct Coordinates
{
    int x, y;
};

enum Color {WHITE, RED, ORANGE, YELLOW, GREEN, BLUE, PURPLE};

class Block
{
    Coordinates _coords;

public:
    // Bonus* bonus;
    // Tipus type;
    Color color;
    
};
